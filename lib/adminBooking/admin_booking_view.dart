import 'package:flutter/material.dart';

class AdminProfileBooking extends StatelessWidget {
  const AdminProfileBooking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('d'),
    );
  }
}
